# MnemoNAS 路线图

[English](roadmap.en.md) | 简体中文

本文档记录 MnemoNAS 从“自托管文件云盘”走向“家庭/小团队 NAS”的产品路线。它用于指导开发优先级，不等同于发布承诺；具体范围会随数据安全、维护成本和用户反馈调整。

## 当前判断

截至 2026-06-14，MnemoNAS 已具备 Web 文件管理、WebDAV、版本历史、回收站、多用户、用户组、用户级容量配额、目录授权、分享、最近操作、健康检查、Scrub、GC、诊断包、Docker 和 systemd 部署、Docker 构建后容器烟测等能力。它已经可以作为家庭/小团队的本地私有文件云盘试用，也可以用于非唯一副本的数据管理。

当前能力可按以下方向概括：

- 公网访问已有第一版基础：反向代理脚本、`mnemonas-public-setup`、`mnemonas-doctor --public-domain`、Web 公网访问向导、安全自检 API、证书续期提示、证书失败排障引导、Web 设置页和 E2E 回归。公网诊断会检查后端端口的宽泛 UFW 放行规则，并支持对存储路径和 WebDAV 用户文件路径中的 `~` 做一致展开。
- 备份与恢复已有本地任务、restic/rclone 命令型远端目标、轻量调度、自动备份时间窗、保留策略自动检测、恢复演练或远端校验、恢复历史、恢复摘要、恢复后只读校验、切换/回滚清单、可复制恢复切换记录、批量恢复待处理原因、首页风险摘要、维护页状态展示，以及 Webhook/Telegram/企业微信/钉钉/SMTP 备份、恢复、恢复校验、恢复演练与保留策略通知。
- 提醒和磁盘健康已有保存后 `alert_test` 测试入口、目录授权和分享策略变更事件、分享即将到期聚合事件，以及基于 `smartctl` 的 SMART、温度、掉盘和序列号漂移检查；这些状态已接入设备状态页、诊断摘要、最近操作和通知。
- 配额和权限已有用户级配额、目录级配额、实际用量统计、账号关注摘要、筛选、排序、复核摘要复制、当前清单导出、目录配额保存前复核，以及 Web/API/WebDAV 写入和恢复路径的配额限制。用户组和目录授权已在 Web/API、WebDAV 用户模式、搜索、分享、收藏、回收站、活动过滤和设置页有效权限检查中复用同一权限判定；分享路径策略也可按创建/维护者用户、组或角色限制。
- 存储可见性已有文件系统容量、文件系统类型、挂载点、设备/数据集来源、脱敏挂载选项、原生数据校验提示和管理员存储健康摘要。SMB 方向已有配置、凭据、网关授权与诊断预览，但当前版本仍不会启动可挂载的 SMB/Samba 运行时；该部分计划等 `smboxide` 项目成熟后再集成。

从完整 NAS 角度看，仍缺少 SMB 可挂载运行时、完整引导式恢复流程、权限继承/分享规则整理、更细的配额/权限管理视图和更完整的存储池管理。因此，在外部备份策略和公网安全检查流程稳定前，不建议把 MnemoNAS 作为唯一长期数据源，也不建议绕过 HTTPS 反向代理直接暴露到公网。

首页首次部署检查已接入认证、分享和 WebDAV 状态摘要，并会在认证关闭或 WebDAV 匿名访问时提示公网部署前处理。该摘要用于补充首次上线提示，不替代安全自检 API、`mnemonas-doctor` 和云防火墙复核。

## 产品定位

MnemoNAS 的优先目标不是复刻 TrueNAS、Synology DSM 或 Unraid 的全部能力，而是成为一个可追踪、可迁移、以原生文件为核心的自托管文件系统入口。

优先级排序：

1. 数据安全和可恢复性优先于功能数量。
2. Web UI 与 WebDAV 体验优先于复杂存储池管理。
3. 可部署、可诊断、可备份优先于全自动化流程。
4. 对家庭/小团队真实工作流有明确价值的能力优先。

## 可用性分级

| 等级 | 目标 | 适用范围 | 必须满足 |
| --- | --- | --- | --- |
| L0 | 开发预览 | 本地调试、演示 | 基本文件读写、启动脚本、单元测试 |
| L1 | 私有文件云盘 | 个人或小团队非唯一副本；默认局域网或受控内网 | Web/WebDAV、认证、版本、回收站、部署文档、外部备份指南 |
| L1+ | 安全远程访问 | 通过域名或反向代理从公网访问 | HTTPS、反向代理预检、安全向导、cookie/CSRF 边界、登录防护、暴露端口检查 |
| L2 | 家庭 NAS 基线 | 局域网长期运行 | SMB、备份任务、SMART/磁盘提醒、通知、配额、恢复演练 |
| L3 | 小团队 NAS | 多用户协作与清晰分享记录 | 文件夹级权限、群组、分享规则整理、活动回顾、灾难恢复流程 |

当前项目已达到 L1 私有文件云盘基线，并具备 L1+ 公网安全入口的第一版基础。下一阶段优先把 L1+ 公网安全检查流程和恢复流程打磨到可长期运行状态，再推进 L2 的数据保护和局域网兼容能力。

## 优先级路线

### P0：可靠私有云盘基线

目标：让 MnemoNAS 可以被谨慎地用于真实个人数据，但前提是用户已有外部备份。

| 能力 | 当前状态 | 下一步 | 验收标准 |
| --- | --- | --- | --- |
| 备份与恢复 | 已有本地/远端备份、恢复预览、恢复执行、恢复后校验、状态摘要和通知基础 | 补更多故障注入覆盖，并继续打磨批量恢复 Web 入口 | 可配置本地目录、外置盘或 rclone/restic 目标；首页和每个任务显示备份、恢复、校验、演练、发现项和提醒状态 |
| 部署可靠性 | Docker/systemd 路径、systemd 升级保留既有配置与数据的成功路径测试、systemd 从上一版本 release 目录回退运行时资产并保留配置/数据的测试、Docker 构建后 loopback 容器烟测、release 安装/卸载安全测试和变更感知验证流程已存在 | 继续固化配置迁移、跨版本演练和真实环境回退记录 | release 包安装、升级、回退、卸载、数据保留和 Docker 镜像健康启动均有自动化测试 |
| HTTPS 与安全向导 | 已有反向代理脚本、Traefik/Cloudflare Tunnel 模板、Web 向导、安全自检 API、doctor 公网/证书/HTTP 跳转检查、证书续期提示、证书失败排障引导、云安全组复核清单和桌面/移动端 E2E 回归 | 继续扩展失败案例和移动端向导覆盖 | 公网域名、局域网自签、反向代理 header、Secure/SameSite cookie、CSRF、下载会话、内部 dataplane 端口未暴露均有自动化测试和文档 |
| 数据完整性 | 有 Scrub/GC/诊断；手动和周期 Scrub 会写入最近操作，失败、对象异常和结果持久化异常会进入 Webhook/Telegram/企业微信/钉钉/SMTP 通知；周期 Scrub 支持失败限次重试，并可在 Web 设置页热更新调度参数，设备状态页和诊断会展示调度状态 | 增加原生 ZFS/Btrfs scrub 协同和更细的失败处置指引 | Scrub 失败能在 UI、最近操作和通知中被发现和追踪 |
| 安全默认值 | Web 主会话使用 HttpOnly cookie，已有登录限速和用户现有登录失效入口 | 完善管理员安全提醒和危险配置提醒 | 弱口令、默认密码、跨站请求、异常登录、公开分享误配置都有明确拦截或提醒 |
| WebDAV 兼容性 | 已有基础矩阵 | 扩展 Windows/macOS/rclone 回归 | 关键客户端读写、重命名、删除、恢复均有记录 |

备份与恢复当前覆盖范围：

- 备份目标：本地任务、本地快照保留策略、restic/rclone 命令型远端目标、轻量调度和自动备份时间窗。
- 恢复流程：本地快照恢复到安全目录、restic/rclone 远端恢复预览与恢复到安全目录、恢复前预检、失败预检拦截、恢复后只读校验、最近恢复重新检查、切换/回滚清单、可复制恢复切换记录和恢复摘要导出。
- 批量入口：批量恢复待处理原因、建议处理步骤、选择、预览、执行前复核与执行。
- 可见性：恢复摘要发现项预览、恢复演练历史、成功率摘要与失败归因、恢复结果记录、匹配恢复校验关联、远端保留策略自动检测、周期化恢复演练状态与过期通知、首页风险摘要和 Web 维护页。
- 通知：Webhook、Telegram、企业微信、钉钉和 SMTP 覆盖备份、恢复、恢复校验、恢复演练和保留策略事件。

### P1：家庭 NAS 基线

目标：补齐家庭局域网 NAS 的基础能力。

| 能力 | 缺口 | 建议实现 | 验收标准 |
| --- | --- | --- | --- |
| SMB/Samba | 已有配置、凭据、网关授权和诊断预览；当前不可挂载 | 等 `smboxide` 成熟后再集成，或提供官方 Samba 侧车方案；优先确保权限和版本边界不被绕过 | Windows/macOS 可直接挂载；权限和路径映射不绕过安全边界；设备状态页和 doctor 能准确提示运行态 |
| 磁盘健康 | 已有 SMART JSON 采集、温度阈值、剩余寿命/介质磨损、设备缺失、序列号不匹配、设备状态页、最近操作和 Webhook/Telegram/企业微信/钉钉/SMTP 提醒 | 增加更多 USB/RAID 桥接兼容性记录 | UI 显示磁盘健康；异常进入最近操作和通知 |
| 通知系统 | Webhook、Telegram、企业微信、钉钉和 SMTP 邮件已覆盖主要容量、备份、恢复、磁盘健康、Scrub、安全和分享事件 | 接入更多运维和安全事件源，并按需支持更多通知通道 | 磁盘满、备份失败、恢复失败或带警告、Scrub 失败、登录限流、配额超限、关键策略变更和分享即将到期能触发通知；管理员可发送测试提醒验证已保存通道 |
| 用户配额 | 已支持用户级配额、目录级配额、用量统计、复核摘要、筛选排序、导出和写入硬限制 | 补更细的趋势和权限结合视图 | 上传、复制、移动、回收站恢复、版本恢复和 WebDAV 写入都遵守适用的用户或目录配额，超限有明确错误和通知 |

通知系统当前覆盖范围：

- 通道：Webhook、Telegram、企业微信、钉钉和 SMTP 邮件。
- 备份与恢复：备份失败/警告、显式恢复失败或警告、恢复后只读校验失败或警告、恢复演练提醒、保留策略检测失败或警告。
- 运行与安全：磁盘空间、磁盘健康异常、Scrub 异常、登录限流、Web/API 配额超限、目录授权/分享策略变更。
- 分享：启用分享即将到期的聚合提醒。
- 测试入口：Web/API 已提供保存配置后的 `alert_test` 测试入口。

用户配额当前覆盖范围：

- 配额模型：用户级容量配额、目录级配额、实际用量统计、目录配额使用率与汇总展示。
- 用户管理：账号关注统计拆分、筛选、原因筛选、按关注排序、用户列表搜索、排序、角色/状态筛选、统计卡快速聚焦、受限用户总配额/余量总览、结果摘要与清除。
- 复核输出：账号复核摘要复制、带联系/权限/登录/余量上下文的摘要复制与卡片状态、用户卡片权限范围与复核提示、用户权限复核摘要复制、复核提示统计与筛选、带复核上下文的当前清单导出。
- 目录配额：目录配额关注清单、筛选、含剩余容量的按关注排序摘要复制、目录配额保存前变更复核。
- 强制限制：Web/API 写入 `home_dir` 的上传、复制、移动、回收站恢复用户配额硬限制，WebDAV 写入 `home_dir` 的 PUT/COPY/MOVE 用户配额硬限制，版本恢复目录配额硬限制，以及 Web/API 配额超限提醒。

### P2：小团队协作

目标：让家庭成员或小团队成员协作更可控，降低误删、误共享和越权风险。

| 能力 | 缺口 | 建议实现 | 验收标准 |
| --- | --- | --- | --- |
| 文件夹级权限 | 已有基础目录授权、用户组、单用户有效权限检查、未保存规则预览、按路径用户矩阵、家庭/小团队策略预设、保存前规则变更复核、目录权限覆盖摘要、相关分享影响检查、可复制权限复核记录和后端持久化近期复核历史；仍缺更细的批量生效明细 | 增加更细的批量生效明细 | Web、API、WebDAV、分享遵守同一权限判定，管理员可查看有效权限 |
| 分享安全 | 已支持新分享默认有效期、默认访问次数、家庭/临时协作策略预设、创建前有效策略复核、按目录路径的强制密码/有效期/访问次数约束、按创建/维护者用户、用户组或角色限制路径分享、分享风险提示、分享策略保存前变更复核、分享策略覆盖摘要、规则整理建议、即将到期提醒、即将到期通知、复核摘要、复核摘要复制、复核筛选、按当前范围跳转复核历史和直接停用高风险分享链接；仍缺更细的复核历史 | 增加更细的复核历史 | 管理员可查看、搜索、禁用所有分享，并能发现长期公开、即将到期或覆盖范围过大的链接 |
| 活动回顾 | 已有最近操作、统计、高风险复核、持久化复核记录、筛选、导出和处置入口 | 增加更多处置执行动作和结果记录 | 可按时间范围、路径/目录、用户和操作类型查看关键活动，便于找回误删、误移动和误分享来源 |
| 管理员恢复工具 | 已有版本、回收站、版本恢复执行前影响复核和恢复结果活动详情、备份批量恢复预检、单任务恢复执行前复核，以及单任务/批量恢复影响摘要 | 增加跨目录恢复和更细的冲突处置记录 | 恢复前显示冲突、覆盖和权限影响 |

活动回顾当前覆盖范围：

- 基础视图：最近操作列表、统计概览、高风险摘要和高风险分组集中窗口复核。
- 复核记录：当前页复核明细、当前页复核处置清单、当前筛选跨页复核记录，以及带处置状态、操作计数、路径样例和用户样例的持久化复核记录。
- 跟进流程：需跟进复核批量视图、需跟进记录处置状态与备注回写、复核记录 CSV 导出、复核历史按复核人/时间/处置状态/关联活动筛选、需跟进复核快速聚焦。
- 追踪入口：单条活动和复核记录可跳转到相关路径、分享或高风险活动；处置入口覆盖版本历史、回收站恢复页面和按路径筛选后的分享处置页。
- 筛选范围：支持按时间范围、路径/目录、分享/高风险分组、操作类型和用户筛选，分享列表可跳转到对应路径的分享活动复核入口，并保留管理员清空入口。

### P3：高级能力

目标：增强长期使用体验，但不应阻塞 L2/L3 基线。

| 能力 | 方向 |
| --- | --- |
| 全文和媒体索引 | 文件名搜索扩展为文档全文、EXIF、视频元数据、重复文件扫描 |
| 同步客户端 | 提供桌面/移动同步或官方 rclone profile，解决离线同步体验 |
| 存储池管理 | 可视化 ZFS/Btrfs/mdraid 状态，必要时调用系统工具而不是重写存储栈 |
| 对象级加密 | 用户密钥、恢复密钥、密钥轮换、备份恢复流程 |
| 插件/自动化 | 基于扩展点增加任务 Runner、Webhook、媒体处理等能力 |

## 推荐实施顺序

1. **完善 HTTPS 和安全向导**：如果计划公网访问，继续降低证书、反向代理、cookie、CSRF、端口暴露、云安全组和默认账号的误配置风险。
2. **备份任务与恢复演练**：确保数据可恢复，再扩大真实使用范围。
3. **磁盘健康与通知**：继续补充 USB/RAID 桥接兼容性，并扩展通知通道。
4. **配额与权限模型**：继续补配额管理视图、趋势、ACL 和用户组，避免多用户长期运行时互相影响。
5. **SMB/Samba 集成**：等 `smboxide` 成熟后再接入可挂载运行时，补齐家庭 NAS 最常见的局域网访问方式。
6. **全文索引、同步客户端、插件**：在数据保护和权限模型稳定后推进。

## 质量门槛

每个 P0/P1 功能合入前至少满足：

- 单元测试覆盖核心边界条件。
- API/存储层集成测试覆盖成功、失败、回滚。
- Web E2E 覆盖桌面和移动端主流程。
- WebDAV 或 SMB 相关能力必须有客户端兼容性记录。
- 公网访问相关能力必须覆盖 HTTPS 证书状态、反向代理 header、Secure/SameSite cookie、CSRF、登录限速和内部端口未暴露检查。
- 涉及数据删除、恢复、备份、配额、权限的功能必须有故障注入或恢复测试。
- 文档必须说明使用边界、失败模式和恢复步骤。

## 非目标

近期不把以下事项作为主线：

- 替代完整 NAS 发行版的磁盘阵列管理。
- 在权限、版本历史、回收站和活动记录边界尚未完整验证前发布自研 SMB/NFS 协议栈。
- 无外部备份前承诺单点长期数据安全。
- 推荐绕过安全预检、doctor 检查和 HTTPS 反向代理的公网直连部署。
- 为公网匿名分享牺牲默认安全边界。

## 路线图维护规则

- 已完成的能力应移入 CHANGELOG 或对应功能文档，不长期堆在路线图中。
- 如果实现方式改变，应更新“验收标准”，而不是只改功能名称。
- 与数据安全相关的条目优先保留明确失败模式和回滚策略。
