# 客户端重构记录

[English](refactor-log.en.md) | 简体中文

本文档记录 Flutter 客户端重构范围、验证证据和剩余工作。记录只反映当前源码状态，不表示已经发布可用版本。

## 进度基线

截至 2026-07-18，完整 Android、Linux 和 Windows 客户端目标的工程就绪度估算为 **60%**。该数值按下表固定权重汇总，用于比较后续变化，不属于发布完成度。

| 工作项 | 权重 | 当前得分 | 当前边界 |
| --- | ---: | ---: | --- |
| 跨平台工程与客户端结构 | 12 | 10 | Flutter 工程、设计系统和 Android/Linux/Windows runner 已建立；桌面原生验证未完成 |
| 认证、会话与上下文隔离 | 14 | 12 | revision/CAS、安全存储、单次 refresh token 轮换及服务器/账号隔离已完成；后台凭据代理未完成 |
| 文件、搜索、回收站与账户流程 | 20 | 15 | 核心文件操作、有界搜索、回收站安全流程和账户流程已接入；版本、分享和管理流程仍不完整 |
| 传输完整性与恢复 | 20 | 11 | 服务端身份条件、客户端持久账本、前台暂停继续、重启恢复和 Android 私有载荷后置发布已完成；上传仍无会话、偏移、幂等提交和结果查询 |
| Android 原生能力与生命周期 | 16 | 8 | SAF 导入导出已实现有界流式复制、进度、取消和 Activity 销毁清理；后台执行器、通知控制、前后台切换和真实设备矩阵未完成 |
| 发布工程与平台安全 | 10 | 3 | debug APK 和基础策略检查可用；独立签名、版本发布轨、release HTTPS 策略和升级验证未完成 |
| Linux 与 Windows 验证 | 8 | 1 | 保留 runner 和共享代码边界；尚无原生构建、运行及分发证据 |
| **合计** | **100** | **60** | **仍处于开发阶段，没有可用版本** |

## 已完成的重构

### 客户端基础与交互

- 建立 Android、Linux 和 Windows 共用的 Flutter 工程、主题与组件边界。
- 接入服务器连接、登录、设备概览、文件浏览、文件名搜索、回收站和账户流程。
- 文档只保留 GitHub Issues 问题反馈入口；README 和开发阶段记录明确说明尚无可用版本。

### 会话与破坏性操作安全

- 会话记录使用 revision/CAS 和原子清除，避免不同 refresh token 代际混用。
- 服务器切换、账号切换、退出和迟到响应使用 context epoch 隔离。
- 删除与回收站流程冻结策略、目标身份和精确 ID；请求终态无法确认时不会推断成功。

### 下载恢复

- 服务端当前文件下载增加 `HEAD`、`X-MnemoNAS-Download-Identity` 和 `X-MnemoNAS-If-Download-Identity`。
- 客户端使用应用私有代际账本保存任务，不保存 access token 或 refresh token。
- 下载使用稳定局部文件和可靠偏移。恢复时必须同时满足对象身份、`206`、精确 `Content-Range` 和总大小。
- 客户端重启后先按局部文件实际长度对账：未完成任务恢复为暂停状态，已完整写入 Android 私有载荷的任务等待重新选择目标，可能已发布到桌面目标但无法确认的任务标记为“结果待确认”。
- Android 先把网络内容提交到应用私有载荷，再进入“等待选择保存位置”。保存对话框不会在网络传输前创建空文档；选定目标后使用当前流程的临时写授权，原生复制成功后才标记完成。客户端重启后保留私有载荷，并要求重新选择位置。

### Android 文件选择

- 上传选择改为原生 `ACTION_OPEN_DOCUMENT`，Dart 只接收 URI 和元数据。
- 原生层以 64 KiB 缓冲区逐个复制文件，执行 `flush` 和 `fd.sync`，失败时清理局部文件。
- 上传导入和下载保存都只使用当前前台流程的临时 URI 授权，不持久占用共享授权。可恢复状态保存在应用私有载荷和账本中，该边界同时避免原插件把完整文件和多选批次载入 Java heap。

### 上传失败语义

- 旧 `POST /api/v1/files/{path}` 明确拒绝 `Content-Range`，防止调用方误认为接口支持分块恢复。
- 上传进入发送阶段后发生连接中断、超时或取消时，客户端保守标记“结果待确认”，不会自动重放可能已经提交的请求。

## 尚未完成

按当前优先级排列：

1. 实现服务端持久上传会话：创建、状态查询、顺序分块、可靠偏移、分块幂等、原子提交、取消、过期清理和终态对账。
2. 把上传任务接入同一持久状态机，并在 Android 中保留 SAF 来源 URI 或安全的应用私有载荷。
3. 实现 Android 原生后台执行器、进度通知、取消与重试动作、系统停止原因处理和跨进程任务租约。
4. 收紧 Android release 明文流量策略，补齐独立签名、版本递增、签名校验、升级安装和 API 24/33/34/36 验证。
5. 完成真实 Android 设备上的大文件、断网、权限撤销、进程终止、前后台切换和升级验收，并复核原生复制失败后由文档提供方保留的空文件或局部文件。
6. 为桌面下载目标发布补充禁止覆盖的原子原语、持久发布日志和目录同步，并持久化重启后的覆盖决策。
7. 在 Linux 和 Windows 原生主机上完成构建、文件选择、目标发布、路径替换和进程恢复验证。
8. 完成版本历史、分享、管理流程、完整多语言和剩余交互一致性检查。

## 记录位置

- 本文档：重构范围、权重进度、已完成项和剩余项。
- [开发阶段变更记录](release-notes.md)：面向当前未发布分支的行为变化与验证摘要。
- [路线图](roadmap.md)：后续阶段、下一步和验收标准。
- [客户端 README](../client/README.md)：当前可见能力、限制和反馈入口。
- [客户端架构](architecture.md)：会话、传输账本和 Android 原生边界。
- [API 参考](api-reference.md)：下载恢复身份与上传接口契约。

## 验证原则

每个重构切片先运行最小定向测试，再运行 `make verify-changed` 和 `make client-check`。Android 相关变更还需要 Kotlin 编译、debug APK、模拟器流程和真实设备证据。debug APK 只用于开发验证，不属于可发布产物。
